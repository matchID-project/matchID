# PoC overlay : same burst-mode treatment as deces-backend.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deces-ui
spec:
  replicas: 0
  template:
    spec:
      nodeSelector:
        k8s.scaleway.com/pool-name: burst
